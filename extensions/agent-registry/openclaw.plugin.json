{
  "id": "agent-registry",
  "channels": ["agent-registry"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "registryUrl": {
        "type": "string",
        "description": "URL of the Agent Registry API"
      },
      "pollIntervalSeconds": {
        "type": "number",
        "default": 300,
        "description": "Interval between inbox/pending checks (seconds)"
      }
    }
  }
}
