/**
 * Agent Registry — setup wizard entry.
 *
 * Minimal setup entry for the channel selection wizard.
 */

import { agentRegistryPlugin } from "./index.js";

export default {
  plugin: agentRegistryPlugin,
};
